# 抗VEGF薬解析（公費レセプトを含まないデータ版）

このフォルダのCSVは、**すべての年度で「公費レセプトを含まない」集計表**を用いて再実行した解析結果です。
親フォルダ（`抗VEFG薬/`）の結果は2024年度のみ「公費レセプトを含む」版を使っており、
時系列に定義の不連続があります。**経年トレンド解析にはこちらを使用してください。**

## 経緯

NDBオープンデータ第11回解説編 p.5:

> 第11回NDBオープンデータでは、…第10回以前との年次推移を観察しやすくするため、
> 第10回までと同様の公費レセプトを含まない集計表も公表した

つまり **第1回〜第10回（2014〜2023年度）はすべて「公費レセプトを含まない」集計のみ**が公表されており、
第11回（2024年度）で初めて「含む」版が標準となり、従来との接続用に「含まない」版が併記されました。

「公費レセプト」の定義（同 p.52 用語の解説）:

> 社会保険・国民健康保険による請求がなく、**公費負担医療のみによる請求のレセプト**

すなわち医療保険の請求が一切ない**公費単独レセプト**（生活保護の医療扶助単独、措置入院等）を指します。
医療保険と公費の併用（難病助成、自立支援医療など）は元々「保険請求あり」なので従来から含まれています。

## 差し替えたデータ

2024年度分のみ差し替え、2014〜2023年度は親フォルダと同一ファイルです。

| 用途 | 使用ファイル |
|---|---|
| 薬剤（都道府県別） | `data/raw/ndb_2024_nokouhi/ndb_chusha_2024_nokouhi.xlsx` |
| 薬剤（年齢性別） | `data/raw/ndb_2024_nokouhi/ndb_chusha_agesex_2024_nokouhi.xlsx` |
| G016（都道府県別） | `data/raw/ndb_2024_nokouhi/ndb_g_chusha_pref_2024_nokouhi.xlsx` |
| G016（年齢性別） | `data/raw/ndb_2024_nokouhi/ndb_g_chusha_agesex_2024_nokouhi.xlsx` |

出典: 第11回NDBオープンデータ
「医科診療行為（算定回数）公費レセプトを含まない」`001712210.zip` /
「処方薬 公費レセプトを含まないデータ」`001711930.zip`

## 再現手順

```bash
python 抗VEFG薬/extract_ophthalmic_drugs.py --nokouhi
```

```bash
python 抗VEFG薬/extract_g016.py --nokouhi
```

```bash
python 抗VEFG薬/run_antivegf_pipeline.py --nokouhi
```

```bash
python 抗VEFG薬/analyze_g016.py --nokouhi
```

```bash
python 抗VEFG薬/analyze_cost.py --nokouhi
```

```bash
python 抗VEFG薬/build_kabata_tables.py
```

解析スクリプトは一旦 `processed/` に出力するため、最後に整理スクリプトを実行して
下記のフォルダ構成へ振り分ける（何度実行しても同じ結果になる）。

```bash
python 抗VEFG薬/organize_outputs.py --nokouhi
```

## フォルダ構成

| フォルダ | 内容 | 件数 |
|---|---|---:|
| `01_抽出データ/` | 生Excelから抽出したCSV（解析の入力） | 5 |
| `02_中間データ/` | 前処理済みデータ・パネルデータ（補完戦略別） | 10 |
| `03_解析結果/` | 解析アウトプット（下記のテーマ別サブフォルダ） | 50 |
| `04_図表/` | 図（`plots/`）・Excel・Word | 12 |
| `05_先行研究再現_Kabata/` | Kabata et al. (2026) の図表再現用データ | 13 |

`03_解析結果/` の内訳:

| サブフォルダ | 主な内容 |
|---|---|
| `全国トレンド/` | 全国処方数量・人口10万対・APC |
| `製品_剤形_バイオシミラー/` | 製品別数量・薬価・剤形比率・先発/BSシェア |
| `医療費/` | 費用の要因分解・治療総額・反実仮想シナリオ・感度分析 |
| `年齢性別/` | 年齢階級×性別の分布（公開粒度版／経年比較版） |
| `都道府県_地域格差/` | 都道府県別集計・格差指標・共変量相関・パネル回帰 |
| `G016硝子体内注射/` | G016算定回数の全国・都道府県・年齢性別解析、薬剤数量との突合 |
| `品質管理_感度分析/` | 秘匿QC・補完戦略の感度分析 |

## 医療費解析について

`03_解析結果/医療費/` の内容は `analyze_cost.py` が生成する。

| ファイル | 内容 |
|---|---|
| `cost_decomposition_yearly.csv` | 費用変化の年次要因分解（数量効果・構成効果・価格効果） |
| `cost_decomposition_cumulative.csv` | 同上の累計 |
| `cost_total_with_procedure.csv` | 薬剤費＋手技料（G016算定回数×点数×10円）の治療総額 |
| `cost_counterfactual_scenarios.csv` | 反実仮想（バイオシミラー非参入／薬価据置） |
| `cost_biosimilar_regional_potential.csv` | BSシェアの地域差解消による追加削減余地 |
| `cost_price_gap_sensitivity.csv` | 実勢価格乖離（0/5/10%）を仮定した感度分析 |

**要因分解の定義**（ΔC = 数量効果＋構成効果＋価格効果、残差0で厳密に分解される）

- 数量効果: (Q_t − Q_t−1) × 前年の加重平均薬価
- 構成効果: Q_t × (当年構成・前年薬価の加重平均 − 前年の加重平均薬価)
- 価格効果: Σ q_it × (p_it − p_i,t−1)

新規収載薬は前年薬価が存在しないため当年薬価で代用しており、参入の影響は
価格効果ではなく構成効果に計上される。

**解釈上の注意**

- 薬価×数量は**薬価基準額**であり、実際の償還額・値引き後価格ではない。
  `cost_price_gap_sensitivity.csv` で乖離率を仮定した幅を示している。
- 転帰データがないため**費用対効果分析（ICER）は算出できない**。
- `cost_biosimilar_regional_potential.csv` は都道府県別BSシェアに基づくが、
  先発の残存数量が少ない県では秘匿の影響でシェアが高めに出るため、
  削減余地はやや過小に見積もられる可能性がある。

## 親フォルダとの差（2024年度）

**2014〜2023年度は完全に一致**（差0）し、差は2024年度のみに限定されることを検証済みです。

| 指標 | 公費含む（親フォルダ） | 公費含まない（本フォルダ） | 差 |
|---|---:|---:|---:|
| 抗VEGF薬 総処方数量 2024 | 1,101,476 | **1,064,756** | −3.33% |
| G016 算定回数 2024 | 1,104,430 | **1,067,660** | −3.33% |
| 薬剤費 2024（億円） | 1,518.7 | **1,467.9** | −3.35% |
| ラニビズマブBSシェア 2024 | 70.75% | **70.83%** | +0.12% |
| 抗VEGF薬合計 APC | +11.39%/年 | **+11.22%/年** | −1.51% |

公費単独レセプトは硝子体注射全体の約3.3%を占めます。
数量・件数・薬剤費がほぼ同率（−3.3%）で減少する一方、**BSシェアなどの構成比はほとんど影響を受けません**。

## 秘匿値の扱い

親フォルダと同一（zero補完を主解析、five/randomで感度分析）。
解説編 p.24 のとおり、秘匿（ハイフン）の位置は「含む」版と「含まない」版で相互に反映されており同一です。
