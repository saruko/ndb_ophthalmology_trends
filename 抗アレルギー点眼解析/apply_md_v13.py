# -*- coding: utf-8 -*-
"""日本語の作業原稿（.md）を v12 → v13 に更新する。

apply_docx_v13.py と同じ改訂のうち、作業原稿に対応する箇所がある分を反映し、
付記15 に改訂の全項目を記録する。作業原稿は本文が改行で折り返されているため、
置換元の照合は改行を無視して行う。

出力: 投稿用v13/論文下書き_総量2014_シェア2022_区間解析_投稿改定ver13.md

実行: python apply_md_v13.py
"""
import io
import os
import re
import sys

BASE = os.path.dirname(os.path.abspath(__file__))
NEW = os.path.join(BASE, "05_論文成果物", "公費含めない_new")
V12 = os.path.join(NEW, "投稿改定ver", "投稿用v12")
V13 = os.path.join(NEW, "投稿改定ver", "投稿用v13")
SRC = os.path.join(V12, "論文下書き_総量2014_シェア2022_区間解析_投稿改定ver12.md")
DEST = os.path.join(V13, "論文下書き_総量2014_シェア2022_区間解析_投稿改定ver13.md")

REPL = []


def R(old, new, n=1):
    REPL.append((old, new, n))


def _pat(old):
    """old の各文字の間に改行（と行頭のインデント）を許す正規表現。"""
    parts = [re.escape(ch) for ch in old]
    return re.compile(r"(?:\n[ \t]*)?".join(parts))


# ---- A. 単位（§2.3） ------------------------------------------------------
R("個＝×0.35（該当はクロモグリク酸Naのユニットドーズ製剤のみ、0.35 mL/個））",
  "個＝×0.35（NDBに掲載された対象品目のうち該当はクロモグリク酸Naのユニットドーズ"
  "製剤のみ、0.35 mL/個。薬価収載されていたがNDBに掲載されなかった品目には"
  "ケトチフェンのユニットドーズ製剤も含まれ、その上限への算入は§2.5））")
R("なお同一成分・同一年度内では単位が揃うため、成分内の先発／後発シェアは換算の有無に依存しない。",
  "なおNDBに掲載された品目については同一成分・同一年度内で単位が揃うため"
  "（クロモグリク酸Naのユニットドーズ製剤（個）と瓶製剤が同一年度に掲載された年度は"
  "ない）、公表値にもとづく成分内の先発／後発シェアは換算の有無に依存しない。"
  "ただし未収載品目の寄与を上限に加算する識別区間の算出はすべてmL換算後に行った"
  "（v13）。")

# ---- B. 第2層の根拠（§2.4） -----------------------------------------------
R("（いずれも「その行の総計は999以下」という公表仕様のみに依拠する）。",
  "（いずれも「その行の総計は999以下」という公表仕様のみに依拠する）。"
  "第1層で採用しなかった「999以下」の上界をここで用いるのは、補完的秘匿が公表された"
  "周辺合計（行の総計）からの差分復元を防ぐために追加のセルを伏せる操作であるのに"
  "対し、総計列にはそれを差分から復元できる上位の集計値（薬効分類ごとの合計など）が"
  "公表されておらず、補完的秘匿の対象となる構造が存在しないためである。この解釈は"
  "公表仕様の読みに依拠しており、実データから直接検証することはできない（v13。付記15）。")

# ---- H. 2023年度の足切り判定（§2.6・§4.6） --------------------------------
R("（2022年度は外来（院内）が規定上限の500品目ちょうどに到達、2023年度は上限未到達だが判定不能）。",
  "（2022年度は外来（院内）が規定上限の500品目ちょうどに到達。2023年度は500品目に"
  "達していないが、第9〜10回の閾値は薬効分類内の医薬品数に応じて上位100・300・500"
  "品目のいずれかに定まり、その医薬品数は公表されていないため、適用された閾値を"
  "特定できず足切りの有無を確定できない）。")
R("2022年度は外来（院内）が上限500品目に到達しており、2023年度は上限未到達だが判定不能である。",
  "2022年度は外来（院内）が上限500品目に到達している。2023年度は500品目未満であるが、"
  "適用された閾値（100・300・500品目のいずれか）を薬効分類内の医薬品数から特定"
  "できないため、足切りの有無を確定できない。")

# ---- I. レボカバスチン後発の非掲載期間 -------------------------------------
R("レボカバスチン（後発品が2021年度以前非掲載）",
  "レボカバスチン（後発品が2014年度は全品目、2015〜2021年度は一部の品目が非掲載）", 3)

# ---- F, L. 性差（§3.2・§4.4） ---------------------------------------------
R("15歳以上では全年齢階級で区間の上限が1を下回り女性優位であった。",
  "15〜94歳では全年齢階級で区間の上限が1を下回り女性優位であった。")
R("最も低い水準は50〜59歳の0.39〜0.43", "最も低い水準は50〜59歳の0.39〜0.42", 2)
R("15歳以上では全階級で女性優位となり、",
  "15〜94歳では全階級で女性優位となり（95歳以上は区間が1をまたぎ判定不能）、")

# ---- O, 図表数値（§2.8・§3.5・§8） ----------------------------------------
R("2014年度で最大37.7ポイント、2021年度で最大19.1ポイント",
  "2014年度で最大35.7ポイント、2021年度で最大19.0ポイント", 2)
R("2021年度以前は品目非掲載の影響で最大38ポイントに達する",
  "2021年度以前は品目非掲載の影響で最大35.7ポイントに達する")
R("**C** の区間幅は0〜4歳で4.9〜8.2ポイント", "**C** の区間幅は0〜4歳で5.0〜8.2ポイント")
R("2014年度の3成分合計は上限が下限の1.9倍に達し", "2014年度の3成分合計は上限が下限の1.8倍に達し")

# ---- P, Q. 提出ファイルとの突合で見つかった不一致（付記15-5） ----------------
R("対象は9成分の全91品目。", "対象は9成分の全95品目。")
R("補完の有無で行が分かれる8品目を含め計99行", "補完の有無で行が分かれる8品目を含め計103行")
R("一致して大域最適が確定し、残る2セルでも両者の差は0.0001以下**で、本稿が示す",
  "一致して大域最適が確定し、残る2セルでも両者の差は0.0002以下**で、本稿が示す")
R("0.0001 以下で、小数第3位までの表示には影響しない。",
  "0.0002 以下で、小数第3位までの表示には影響しない。")
R("| アシタザノラスト | 6.46 | 5,215–5,219 | 2.7 | 0 |",
  "| アシタザノラスト | 6.46 | 5,215–5,219 | 2.7–2.8 | 0 |")
# 0.1%製剤の年齢別割合を docx と同じ「識別区間の中点」に揃える（Suppl Table S11）
R("""2024年度の0.1%製剤の割合は年齢によっても異なり、0〜4歳83.0%・5〜9歳82.7%・
10〜14歳78.7%と0〜14歳で高く、15〜19歳70.3%、20〜69歳は約60〜64%、70〜84歳は""",
  """2024年度の0.1%製剤の割合は年齢によっても異なり、0〜4歳80.8%・5〜9歳82.5%・
10〜14歳78.5%と0〜14歳で高く、15〜19歳70.0%、20〜69歳は約59〜63%、70〜84歳は""")

# ---- K. 主要知見（5）・結論（§4.1・§6） ------------------------------------
R("（5）全国集計に対するセル秘匿の影響は2022年度以降無視できるほど小さい一方",
  "（5）全国集計に残る秘匿の影響（総計秘匿、2024年度で最大0.111%）は2022年度以降"
  "無視できるほど小さい一方")
R("転じる中でエピナスチンのみが増加を続け、主要3成分で85%前後、とりわけエピナスチン単独で処方量の半数を占めるに至った。",
  "転じる中でエピナスチンのみが増加を続け、主要3成分が83〜85%を占め、2024年度には"
  "エピナスチン単独で処方量の半数を占めるに至った。")
R("男女差は0〜14歳で男性優位、15歳以上で一貫して女性優位であり、都道府県間には3.1倍の開きがある。"
  "NDBオープンデータのセル秘匿は2022年度以降の全国集計の結論に影響しない（最大0.111%）一方、",
  "男女差は0〜14歳で男性優位、15〜94歳で一貫して女性優位（95歳以上は判定不能）であり、"
  "都道府県間には少なくとも2.6倍の開きがあった。NDBオープンデータの秘匿が2022年度"
  "以降の全国集計に残す不確実性は総計秘匿による最大0.111%にとどまり結論に影響しない一方、")

APPENDIX = """

---

## 付記15：2件の外部レビューで指摘された矛盾の解消（v13・2026-09-07）

v13 は数値の再計算を伴わない。図表ファイル間の突合（Word・PPT・Excel）と、原稿内部の
論理・記述の突合の2件のレビューで指摘された矛盾を解消した。投稿用 docx
（`投稿用v13/NDB抗アレルギー解析_v13_clean.docx`）が正であり、本作業原稿には対応する
箇所がある分のみ反映した（引用番号・Suppl Table S2 凡例・欠落率の記述は docx のみ）。

### 15-1. 図表ファイルのキャプション（PPT が古い数値を引きずっていた）

| 箇所 | 誤（PPT・本作業原稿） | 正（Word・データ） | 根拠 |
|---|---|---|---|
| Fig 4A 2014年度の上限倍率 | 1.9x | **1.8x** | 200.7／109.1＝1.84 |
| Fig 1C 0〜4歳の区間幅 | 4.9–8.2 pp | **5.0–8.2 pp** | レボカバスチン 8.2147−3.2592＝4.96 |
| Fig 4B 2014年度の最大区間幅 | 37.7 pp | **35.7 pp** | Suppl Table S8 `Fig4B_top3` レボカバスチン 35.68 |
| 同 2021年度 | 19.1 | **19.0** | 同 エピナスチン 18.99 |

`build_submission_v11.py`・`build_figure_bundle.py` の文字列を訂正し、
`Figure4.pptx`・`figureまとめ_v13_提出.pptx` にも反映した。

### 15-2. 原稿内部の矛盾（docx で修正、該当箇所は本作業原稿にも反映）

1. **単位の記述の自己矛盾**（§2.3 vs §2.5）。「個＝クロモグリク酸Naのみ」は NDB に
   掲載された品目についての記述で、未収載品目にはケトチフェンのユニットドーズ製剤
   （ザジテンUD。全期間 NDB 未掲載）が含まれる。掲載品目／未収載品目を区別して書き分け、
   「成分内シェアは換算に依存しない」を公表値ベースに限定した（識別区間の算出は
   mL換算後）。クロモグリク酸Na の UD（個）と瓶製剤が同一年度に掲載された年度はない
   （UD は2015〜2018年度、瓶の後発品は2022〜2024年度）。
2. **第1層で棄却した「999以下」を第2層で使う根拠**。補完的秘匿は公表された周辺合計から
   の差分復元を防ぐ操作であり、総計列には復元の元になる上位集計（薬効分類合計など）が
   公表されていないため対象となる構造がない。この解釈が公表仕様の読みに依拠し実データ
   で検証できないことを Methods と Limitations に明記した。
3. **上限の偏りの向き**。Suppl Table S2 凡例の「上限の過小評価にはつながらない」は誤りで、
   未収載品目数が下限なら上限は「過大評価にはならないが過小評価となりうる」。本文の
   「上限自体も下限である」と揃えた。
4. **引用番号**。性差逆転のメタ解析は文献15（Fröhlich）・16（Pinart）。§4.4 の 14,15 を
   15,16 に訂正（文献14は Urashima の花粉症論文）。
5. **Figure 6A の開始年度**。§2.6 の「全期間を図示」を、Fig 6A は2015年度以降、
   Suppl Fig S2 は2014年度以降、と書き分けた。
6. **「15歳以上で全階級女性優位」**は 95〜99歳（0.48〜2.42）・100歳以上（0.09〜39.4）で
   区間が1をまたぐため過剰主張。「15〜94歳」に限定（§3.2・§4.4・§6）。
7. **欠落率の傾向**。「収録品目数の多い年度で大きく」は自分の数値（2022〜2024年度が
   1.30〜1.45%で2017年度4.94%より小さい）と逆。「処方量の小さい成分が掲載された年度で
   大きく」とし、数値を降順に並べ替えた。
8. **2023年度の足切り判定**。第9〜10回の閾値は薬効分類内の医薬品数で 100／300／500 の
   いずれかに定まり、その医薬品数は非公表なので「500品目未到達」だけでは足切りの有無を
   確定できない。この論理を Methods・Limitations に書いた。
9. **レボカバスチン後発品の非掲載期間**。Suppl Table S2・S9 のとおり2014年度は全9品目、
   2015〜2021年度は一部（2〜3品目掲載、7〜9品目非掲載）が非掲載。「2021年度以前非掲載」
   を「2014年度は全品目、2015〜2021年度は一部」に統一（§2.8、Fig 6・Fig 7 脚注）。
10. **未収載品目数の同定方法**を Methods「第一に」で1回定義し（薬価基準収載品目リスト＋
    製品情報、銘柄単位、「◯社以上」は下限）、§2.5 と S2 凡例はそれを参照する形にした。
    §2.5 末尾の重複文（2022〜2024年度は未収載品目ゼロ）を削除。
11. **Conclusion の層の帰属**。全国集計は総計列を使うためセル秘匿は効かず、残る 0.111% は
    総計秘匿由来。「セル秘匿」→「秘匿（総計秘匿）」に訂正（§4.1 主要知見(5)・§6）。
12. **「主要3成分で85%前後」**は Table 3 では 83.1〜83.2／84.1／85.4〜85.5%。
    「83〜85%」に訂正。
13. **男／女比の最低水準**。Table 2 は 50〜54歳 0.39–0.42、55〜59歳 0.40–0.42（上限
    0.4245）。「0.39〜0.43」→「0.39〜0.42」。
14. **文献19・20**。Ventura（高齢者アレルギー疾患の免疫学）は「加齢に伴う病態の変化」に、
    Fujishima 2014（スギ花粉抗原誘発試験）は「エピナスチンの有効性」に付け替え、
    「定期通院」「アドヒアランス改善」には引用を付けない。
15. **Geographic 節末尾の花粉の2文**（ジニ係数の段落に接続していない）を Figure 3B の
    地理的勾配の記述に移し、Discussion の重複を「（Results参照）」に整理。
16. Methods の「最大36ポイント」を Suppl Table S8 の 35.7 に合わせた。

### 15-3. 提出ファイル4点と原稿の突合で見つかった不一致

投稿用 docx と `Tableまとめ`・`SupplTableSまとめ`・`SupplFigureSまとめ`（xlsx・pptx）の
数値を機械的に突き合わせた（229項目）。以下の4件を直した。

1. **Table 1 アシタザノラストの9成分中シェアが単一値になっていた。** Suppl Table S8 の
   上限 2.7515% は他行と同じ小数第1位の丸めでは 2.8 であり、単一値ではなく
   **2.7–2.8** が正しい（下限 2.7465 → 2.7）。
2. **Suppl Table S1 の凡例の品目数・行数が実ファイルと合わない。** 実ファイルは
   95品目・103行（うち単位を後年度から補完した8品目が2行に分かれる）。
   凡例の「91品目・99行」を **95品目・103行** に訂正した。
3. **ジニ係数の最大値の証明ギャップ。** 「残る2セルでも両者の差は0.0001以下」と
   書いていたが、実測の最大は 0.000123（レボカバスチン2022年度。Suppl Table S10 の
   `gini_max_gap` 列）で、上界の主張として成立しない。**0.0002以下** に訂正した
   （小数第3位までの表示に影響しないという結論は変わらない）。生成元
   `apply_docx_v12.py` が `f"{gap:.4f}"` で切り下げていたため、同じ誤りが再発しないよう
   切り上げに変えた。
4. **`SupplFigureSまとめ` の Notes シートが v11 のままだった。**
   「manuscript v11」→ v13、「Generated by build_supplfig_en_v11.py」→ v12、
   および **「1st-10th releases (FY2014-FY2024)」→「1st-11th releases」**
   （FY2014–FY2024 は第1回〜第11回。本文の記述と食い違っていた）。生成元
   `build_supplfig_en_v12.py` も直した。

あわせて、本作業原稿だけに残っていた **0.1%製剤の年齢別割合**（§3.5・§4.3）を
docx と同じ識別区間の中点に揃えた（0〜4歳 83.0→80.8%、5〜9歳 82.7→82.5%、
10〜14歳 78.7→78.5%、15〜19歳 70.3→70.0%、20〜69歳 約60〜64→約59〜63%）。
docx は中点に更新済みで、本ファイルだけが公表値のままだった。

### 15-4. 指摘のうち修正しなかったもの

- Table 1 の処方量が小数第2位表記のため、アシタザノラスト・イブジラストが単一値になる点。
  凡例に「区間幅が表示桁未満の場合は単一値」と明記済みで、人口10万対の列に区間が出る
  ことと矛盾しない。

### 15-5. 再生成

```
python apply_docx_v13.py     # 原稿 v13（履歴つき／clean）
python apply_md_v13.py       # 本ファイル
python build_v13_folder.py   # 図表・まとめファイルを v12 から複製（名前を v13 に）
python verify_v13.py         # 検証
```
"""


def main():
    md = io.open(SRC, encoding="utf-8").read()
    miss = []
    for old, new, n in REPL:
        pat = _pat(old)
        c = len(pat.findall(md))
        if c != n:
            miss.append((c, n, old[:50]))
            continue
        md = pat.sub(lambda m: new, md)
    if miss:
        for c, n, o in miss:
            print(f"  NG  count={c} expected={n}  {o}")
        raise SystemExit("置換元が見つからない箇所がある")
    md = md.rstrip("\n") + "\n" + APPENDIX
    os.makedirs(V13, exist_ok=True)
    io.open(DEST, "w", encoding="utf-8", newline="\n").write(md)
    print(f"-> {DEST}")
    print(f"   {len(REPL)} 箇所を置換、付記15 を追加（{len(md):,} 文字）")


if __name__ == "__main__":
    sys.stdout.reconfigure(encoding="utf-8")
    main()
